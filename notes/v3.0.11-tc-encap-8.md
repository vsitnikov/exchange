# v3.0.11-tc-encap-8 notes

Source checkout: `/workspace/packetveil`

Branch: `cursor/tc-encap-adjust-room-3a8e`

New commit:

```text
cafecb3 Finalize GUT data length metadata with direct write
```

Patch artifact:

```text
patches/gutd-finalize-gut-data-len-direct-write.patch
```

Patch effect:

- Keeps existing v7 GUT type-4 metadata encode/decode scheme.
- Moves final TC egress metadata byte write to a direct packet write at the actual final GUT header location: `udp_off + sizeof(struct udphdr) + 8`.
- This write occurs after dynamic peer endpoint/IP/UDP port rewrites, checksum updates, and Ethernet MAC rewrite.
- Avoids `bpf_skb_store_bytes()` pointer invalidation and avoids relying on saved `new_quic_off`.

Validation in Cursor Cloud:

```text
GUT_VERSION=v3.0.11-tc-encap-8 cargo build --release
cargo test --release
```

Result: build passed; 24 library tests and 13 integration tests passed.

Local glibc artifact:

```text
artifacts/gutd-x86_64-v3.0.11-tc-encap-8
```

SHA256:

```text
4c9935b5bea83e308656ac7130d88a542edad4e82193c986a3e3f83aa90d1523
```

Use GitHub Actions in `vsitnikov/packetveil` for the final musl-static deployment binary.
