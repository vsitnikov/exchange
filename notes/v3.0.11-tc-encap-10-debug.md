# v3.0.11-tc-encap-10-debug notes

Source checkout: `/workspace/packetveil`

Branch: `cursor/tc-encap-adjust-room-3a8e`

New commit:

```text
e070bd1 Add GUT type4 TC egress debug counters
```

Patch artifact:

```text
patches/gutd-tc-type4-debug-counters.patch
```

Files modified:

- `src/bpf/gut_common.h`
- `src/bpf/tc_gut_egress.bpf.c`
- `src/tc/maps.rs`
- `src/tc/loader.rs`
- `src/main.rs`

Patch effect:

- Adds `tc_debug_map` with TC type-4 GUT egress counters and last-value fields.
- TC egress increments counters for total packets, GUT mode, WG type distribution, type-4 fixed/dynamic peer, metadata attempts/success/fail, final byte8 readback, and last offset/wg_len/len_code/byte8.
- TC egress emits narrow `bpf_printk()` for GUT type-4 `wg_len == 128`.
- Userspace reads the debug map and prints `tc_dbg ...` lines via `print_bpf_stats()` and periodic stats dump path, so `journalctl -u gutd-stage-direct.service` can show counters.

Validation in Cursor Cloud:

```text
GUT_VERSION=v3.0.11-tc-encap-10-debug cargo build --release
cargo test --release
```

Result: build passed; 24 library tests and 13 integration tests passed.

Local glibc artifact:

```text
artifacts/gutd-x86_64-v3.0.11-tc-encap-10-debug
```

SHA256:

```text
74cc1b47b64c501d4966c812f7c55268d50709a174154be3d4581f9775e962fe
```

Use GitHub Actions in `vsitnikov/packetveil` for the final musl-static deployment binary.
