# v3.0.11-tc-encap-4 notes

Source checkout: `/workspace/packetveil`

Branch: `cursor/tc-encap-adjust-room-3a8e`

New commit:

- `cc122ab Restore canonical GUT handshake lengths on XDP ingress`

Patch effect:

- Only `GUT_MODE_GUT` XDP ingress canonicalizes WireGuard handshake message lengths after deobfuscation.
- Type 1 handshake initiation restores to 148 bytes.
- Type 2 handshake response restores to 92 bytes.
- Type 3 cookie reply restores to 64 bytes.
- Type 4 data messages keep variable length and continue to use ballast-based trimming.
- XDP tail is physically trimmed before `bpf_xdp_adjust_head()`, then IPv4/IPv6 total/payload and UDP lengths are set from the restored payload length.
- QUIC/SIP/syslog builds retain their previous length logic.

Validation run in Cursor Cloud:

```text
GUT_VERSION=v3.0.11-tc-encap-4 cargo build --release
cargo test --release
```

Result: build passed; 24 library tests and 13 integration tests passed.

Local artifact:

```text
artifacts/gutd-x86_64-v3.0.11-tc-encap-4
```

This local artifact is an x86_64 glibc-linked release build, not the upstream GitHub Actions musl-static release binary. Use GitHub Actions in `vsitnikov/packetveil` for the final deployment asset.

SHA256:

```text
d8134d7a8ff4c230188189355c52ebd351426cac1a4bd58259f967f039c0e538  /workspace/release-out-v4/gutd-x86_64-v3.0.11-tc-encap-4
```
