# v3.0.11-tc-encap-5 notes

Source checkout: `/workspace/packetveil`

Branch: `cursor/tc-encap-adjust-room-3a8e`

New commit:

```text
9e11edc Defer GUT handshake tail trim until after decapsulation
```

Patch artifact:

```text
patches/gutd-defer-gut-handshake-tail-trim.patch
```

Patch effect:

- Reverts the unsafe early `bpf_xdp_adjust_tail()` introduced by the first v4 tail-trim approach.
- Keeps GUT-mode canonical WireGuard handshake restored lengths:
  - type 1: 148 bytes
  - type 2: 92 bytes
  - type 3: 64 bytes
  - type 4: variable length
- Defers physical tail trimming to the existing post-`bpf_xdp_adjust_head()` trim point, after the GUT envelope has been removed and header pointers are revalidated.
- Adds `tail_total <= 63` bound for verifier-friendly trimming.

Validation in Cursor Cloud:

```text
GUT_VERSION=v3.0.11-tc-encap-5 cargo build --release
cargo test --release
```

Result: build passed; 24 library tests and 13 integration tests passed.

Local glibc artifact:

```text
artifacts/gutd-x86_64-v3.0.11-tc-encap-5
```

SHA256:

```text
7d3c82bebafec795de0ec15547294b9deed5ea66d9385aa48b3f51c89f9161b9
```

Use GitHub Actions in `vsitnikov/packetveil` for the final musl-static deployment binary.
