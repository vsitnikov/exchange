# v3.0.11-tc-encap-3 notes

Source checkout: `/workspace/packetveil`

Branch: `cursor/tc-encap-adjust-room-3a8e`

Local commits on top of `origin/cursor/tc-encap-adjust-room-3a8e`:

- `c76b872` merge of upstream main containing:
  - `cb0195f recalculate bind_ip, dst_mac in 60s for stability`
  - `eaa1f28 Issue #12 fixed`
  - `46e2c18 Issue #12: roaming fix`
  - `3b055f7 Issue #12: sip fix`
- `afa43e9 Zero outer IPv4 UDP checksum in TC GUT egress`

Patch effect: main TC egress IPv4 outer UDP emission now sets `udph->check = 0` for the final outer IPv4 UDP packet instead of writing a pseudo-header checksum. IPv6 and separate SIP/QUIC tail-call checksum paths are unchanged.

Validation run in Cursor Cloud:

```text
cargo build --release
cargo test --release
```

Result: build passed; 24 library tests and 13 integration tests passed.

Artifact included here:

```text
artifacts/gutd-x86_64-v3.0.11-tc-encap-3
```

This local artifact is an x86_64 glibc-linked release build, not the upstream GitHub Actions musl-static release binary. Use GitHub Actions in `vsitnikov/packetveil` for the final server deployment asset.

SHA256:

```text
92904a7c56b025dddcfc910cc5fb9efabfc519b649458d82888f22dcd9a03e66  /workspace/release-out/gutd-x86_64-v3.0.11-tc-encap-3
```
