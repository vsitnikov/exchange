# v3.0.11-tc-encap-9 notes

Source checkout: `/workspace/packetveil`

Branch: `cursor/tc-encap-adjust-room-3a8e`

New commit:

```text
9eaf906 Finalize GUT type4 metadata with full header rewrite
```

Patch artifact:

```text
patches/gutd-finalize-gut-type4-full-header-rewrite.patch
```

Patch effect:

- Keeps v7/v8 type-4 length metadata scheme.
- Finalizes the entire 10-byte GUT header for type-4 packets at the final on-wire GUT payload offset: `udp_off + sizeof(struct udphdr)`.
- Rewrites PPN, encrypted ports, length metadata byte 8, and padding byte 9 in a single late `bpf_skb_store_bytes()` call.
- This finalization happens after dynamic-peer IP/UDP rewrites, checksum updates, and Ethernet MAC rewrite.
- Avoids relying on saved `new_quic_off` or direct byte store.

Validation in Cursor Cloud:

```text
GUT_VERSION=v3.0.11-tc-encap-9 cargo build --release
cargo test --release
```

Result: build passed; 24 library tests and 13 integration tests passed.

Local glibc artifact:

```text
artifacts/gutd-x86_64-v3.0.11-tc-encap-9
```

SHA256:

```text
464213e5f01ecda9b711b33c5e10fd6b0933e4194a2dee184074ae3224ee7995
```

Use GitHub Actions in `vsitnikov/packetveil` for the final musl-static deployment binary.
